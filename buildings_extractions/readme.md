1. split_building_tiles.py
Code to split raster into tiles (256, 512, or 1024) which are input into SAM3.

2. batch_segment_sam3.py
Run code in QGIS using GeoAI plugin to batch classify tiles created by split_building_tiles.py using SAM3 and a text prompt.
